由于编程能力的限制，本小组并未实现可下载的软件编写，而仅在 Web 端实现了软件的核心功能。具体运行方式如下：

1. 将压缩包内所有文件下载到本地，并在本地编译器中打开本文件夹
2. 打开 PowerShell ，并选择“以管理员模式运行”
3. 在 PowerShell 中 cd 到文件夹“code”所在的地址
4. 运行指令“npm start”

即可在默认浏览器中运行本小组实现的软件界面与功能。